# Configuración local que no se sube al repositorio
APP_ENV=dev
CORS_ALLOW_ORIGIN='^https?://(localhost|127\.0\.0\.1)(:[0-9]+)?$'
FRONTEND_URL=http://localhost:5173

# Aquí puedes sobrescribir cualquier otra variable local como
# credenciales de base de datos para tu entorno específico
